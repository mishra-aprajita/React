# Notes

## Hooks 

1.Hooks are function 
2.Hooks allows to use state are other features without writinh a class. 

>Rule to use Hooks 
1.Only call Hooks at the top level:
2.Only call hooks from React functions:

>React components can't re-render the normal defined variables 